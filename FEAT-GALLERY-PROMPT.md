I wanna create a new section of the website (that is in a sepearte view and not part of the main page scrolling) called 'Gallery' that should be clicable in the nav bar, next the 'Staff' button and before the 'Contact' button

from a UI perspective it should be a simple, modern, easy to use page that acts much like a photo gallery on the phone

The way it should work is that each 'album' in the gallery is IDEALLY tied to an event in the calendar... and of course each 'album' is tagged with a specific date, that is, the date of the event it is tied to, oir a manually set date. The UI should allow to sort albums by most recent date or oldest date

But as you design this, for the actualy photos just put some stock photo stub, because I need your help on how do I actually drop photos in here. I don't want to start dumping more and mor epictures into the repo itself as that would be a prvacy concern and a bloated source code. it's basically bad practice... so I need your recommendations on what are my options for integrating images into my site